<%= render 'layouts/navbar' %>
  <section class="container-fluid bg-warning vh-auto pb-5" style="padding-top: 11rem">
    <div class="container">
      <%= render 'shared/alert_messages' %>
      <header class="text-center mb-3">
        <h2 class="fw-semibold"><%= t('header.sell_property') %></h2>
      </header>
      <div class="row justify-content-center">
        <div class="col-md-5">
          <%= render 'form', property_request: @property_request %>
        </div>
      </div>
    </div>
  </section>
<%= render 'layouts/footer' %>
